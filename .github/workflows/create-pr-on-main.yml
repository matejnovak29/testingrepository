name: Create PR in Client's Repo with Terraform Setup

on:
  workflow_dispatch:  # Manual trigger

jobs:
  create-pr:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Check out your repository (Repo A)
      - name: Checkout Repo A
        uses: actions/checkout@v3

      # Step 2: Set up Terraform
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: "1.3.0"

      # Step 3: Terraform Init and Apply
      - name: Initialize and Apply Terraform
        env:
          TF_VAR_dbt_cloud_api_token: ${{ secrets.DBT_CLOUD_API_TOKEN }}
        run: |
          terraform init
          terraform apply -auto-approve | tee tf_output.log
          tail -n 50 tf_output.log

      # Step 4: Extract Job ID from Terraform Output
      - name: Extract Job ID
        id: extract_job_id
        run: |
          job_id=$(terraform output -raw dbt_cloud_job_id)
          echo "Job ID: $job_id"
          echo "::set-output name=job_id::$job_id"

      # Step 5: Replace "DAG" with Job ID in the Airflow DAG Template
      - name: Replace Job ID in Template
        run: |
          job_id=${{ steps.extract_job_id.outputs.job_id }}
          sed -i "s/DAG/$job_id/g" templates/airflow_dag.py

      # Step 6: Clone Client Repo (Repo B)
      - name: Clone Client Repo (Repo B)
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          git clone https://${GH_PAT}@github.com/matejnovak29/testingrepository2.git client-repo

      # Step 7: Copy Airflow DAG Template to Client Repo
      - name: Copy Template to Client Repo
        run: |
          # Ensure the target directory exists
          if [ ! -d "client-repo/airflow_dbt_cloud_dags" ]; then
            mkdir -p client-repo/airflow_dbt_cloud_dags
          fi

          # Copy the updated template to the target directory
          cp templates/airflow_dag.py client-repo/airflow_dbt_cloud_dags/

      # Step 8: Commit and Push Changes to a New Branch in Client Repo
      - name: Commit and Push Changes to Client Repo
        run: |
          cd client-repo

          # Create and switch to a new branch
          git checkout -b feature/airflow-dag-template

          # Add the copied file
          git add airflow_dbt_cloud_dags/airflow_dag.py

          # Commit changes
          git commit -m "Add Airflow DAG Template with Job ID"

          # Push the new branch to the client repository
          git push origin feature/airflow-dag-template

      # Step 9: Create Pull Request in Client Repo
      - name: Create Pull Request in Client Repo
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}  # Map your GH_PAT token to GH_TOKEN
        run: |
          gh pr create \
            --repo matejnovak29/testingrepository2 \
            --title "Add Airflow DAG Template with Job ID" \
            --body "This PR adds the Airflow DAG Template with the dynamically generated Job ID from Terraform." \
            --base main \
            --head feature/airflow-dag-template
